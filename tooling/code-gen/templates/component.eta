import { installer } from "../../shared/installer"

export default class <%~ it.newComponentClassName %> extends HTMLElement {
  constructor () {
    super()
  }
}
// handle umd installs
installer("<%~ it.flowplayerComponentName %>", "<%~ it.newComponentHTMLName %>", <%~ it.newComponentClassName %>)
